customer_id = 1001  # random number
customer_name = "Maria Lopez" # full name, can be split as first and last name
customer_gender = "F" # single letter code for gender
customer_dob = "1998-03-15" # stored as string. can convert to a date type if calculations needed.
license_number = "DL123456" # string, contains letters
policy_number = "POL-01234" # string , contains letters and dashes

my_name = "Cagla Celik"
hometown = "Chicago, IL"

# Reserved words that cannot be used as variable names:
# False, None, True, and, as, assert, async, await,
# break, class, continue, def, del, elif, else, except,
# finally, for, from, global, if, import, in, is,
# lambda, nonlocal, not, or, pass, raise, return,
# try, while, with, yield

# if: used to start a conditional block, runs code only when a condition is True
# for: used to loop through items in a collection one by one
# and: combines two conditions, both must be True for the block to run
# def: used to define/create a reusable function
# return: used inside a function to send a value back out